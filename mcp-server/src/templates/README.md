Place the official workbook in this directory as:

`ProductionPlanTemplate.xlsx`

The MCP server reads that file at runtime and treats its worksheet names and detected
column headers as authoritative. The template itself is intentionally not synthesized
by the server.
